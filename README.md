# net-sniffer
Net Sniffer captures and displays network traffic from a specified interface in real time directly in your terminal, so you can inspect how packets are sent and received across your LAN and the public internet.
